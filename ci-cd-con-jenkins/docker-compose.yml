services:
  jenkins:
    build: .
    container_name: jenkins-demo
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
      - ./app:/mnt/app:ro

volumes:
  jenkins_home:
