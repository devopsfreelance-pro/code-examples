services:
  anvil:
    image: ghcr.io/foundry-rs/foundry:latest
    container_name: redes-prueba-anvil
    entrypoint: ["anvil"]
    command:
      - "--host"
      - "0.0.0.0"
      - "--chain-id"
      - "31337"
      - "--block-time"
      - "2"
    ports:
      - "8545:8545"
