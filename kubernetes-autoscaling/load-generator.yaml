apiVersion: v1
kind: Pod
metadata:
  name: load-generator
  labels:
    app: load-generator
spec:
  containers:
  - name: busybox
    image: busybox
    command:
    - /bin/sh
    - -c
    - "while true; do wget -q -O- http://php-apache.default.svc.cluster.local; done"
  restartPolicy: Never
