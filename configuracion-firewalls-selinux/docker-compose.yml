services:
  firewall-selinux-lab:
    build: .
    container_name: firewall-selinux-lab
    cap_add:
      - NET_ADMIN
      - NET_RAW
      - SYS_ADMIN
    security_opt:
      - seccomp:unconfined
      - apparmor:unconfined
    tty: true
