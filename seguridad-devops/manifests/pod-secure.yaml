apiVersion: v1
kind: Pod
metadata:
  name: app-secure
  labels:
    app: demo
spec:
  containers:
    - name: app
      image: nginx:1.27
      securityContext:
        runAsNonRoot: true
        privileged: false
        readOnlyRootFilesystem: true
