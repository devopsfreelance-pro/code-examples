services:
  localstack:
    image: localstack/localstack:3.4
    container_name: tf-localstack
    ports:
      - "4566:4566"
    environment:
      - SERVICES=ec2,iam,sts
      - DEBUG=0
    volumes:
      - "localstack-data:/var/lib/localstack"

volumes:
  localstack-data:
